extends layout

block content

  h1 著者: #{author.name}
  p #{author.lifespan}

  div(style='margin-left:20px;margin-top:20px')

    h2(style='font-size: 1.5rem;') 本
    if author_books.length
      dl
        each book in author_books
          dt
            a(href=book.url) #{book.title}
          dd #{book.summary}
    else
      p この著者の本はありません。

  hr
  p
    a(href=author.url+'/delete') 著者を削除
  p
    a(href=author.url+'/update') 著者情報を編集
